{
  "name": "shhans-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ shhans-test",
    "create-local": "graph create --node http://localhost:8020/ shhans-test",
    "remove-local": "graph remove --node http://localhost:8020/ shhans-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 shhans-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
